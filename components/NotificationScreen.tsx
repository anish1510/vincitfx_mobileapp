import { View, Text } from "react-native";

function NotificationScreen({navigation}): React.JSX.Element {

    return (
        <View>
            <Text>NotificationScreen</Text>
        </View>
    );
  }
  
  export default NotificationScreen;