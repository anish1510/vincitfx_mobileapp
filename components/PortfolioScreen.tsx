import { View, Text } from "react-native";

function PortfolioScreen({navigation}): React.JSX.Element {

    return (
        <View>
            <Text>PortfolioScreen</Text>
        </View>
    );
  }
  
  export default PortfolioScreen;